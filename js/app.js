(function() {
  var app = angular.module('main', []);

  /*
   *  setup api to interact with  map
   */
  app.controller('gameController', [function($scope) {
    
  }])

  app.factory('map', [function() {
    var obj = {

    }

    return obj;
  }])


})
